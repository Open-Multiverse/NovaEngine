---
phase: 01-测试基础设施修复
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nova_character/src/character.rs
  - crates/nova_character/src/state.rs
autonomous: true
requirements:
  - TESTFIX-01
must_haves:
  truths:
    - "CharacterStats::new('x').with_health(100.0).with_attack(20.0).with_defense(10.0) 可构造"
    - "CharacterBundle::default() 可构造，包含 stats/state/transform/visibility 四个字段"
    - "CharacterState::current() 返回对自身的引用，matches!(state.current(), CharacterState::Idle) 成立"
  artifacts:
    - path: "crates/nova_character/src/character.rs"
      provides: "CharacterStats 组件、CharacterBundle Bundle"
      exports:
        - "pub struct CharacterStats"
        - "pub struct CharacterBundle"
    - path: "crates/nova_character/src/state.rs"
      provides: "CharacterState::current() 方法"
      contains: "pub fn current"
  key_links:
    - from: "CharacterBundle"
      to: "CharacterStats"
      via: "pub stats: CharacterStats 字段"
      pattern: "pub stats: CharacterStats"
    - from: "CharacterStats"
      to: "Attributes"
      via: "pub attributes: Attributes 字段"
      pattern: "pub attributes: Attributes"
---

<objective>
在 nova_character crate 中补齐两个缺失类型（CharacterStats、CharacterBundle）并为 CharacterState 添加 current() 方法，使集成测试 character_tests.rs 可以编译并通过。

Purpose: 这是阻塞 CI 的根本原因之一——集成测试引用了尚不存在的类型。补齐后 Wave 2 的测试挂载计划才能运行。
Output: 修改后的 character.rs（新增 CharacterStats + CharacterBundle）和 state.rs（新增 current() 方法）
</objective>

<execution_context>
@/Users/fwind/dosec/AI产品/game_engine/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fwind/dosec/AI产品/game_engine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/fwind/dosec/AI产品/game_engine/.planning/PROJECT.md
@/Users/fwind/dosec/AI产品/game_engine/.planning/ROADMAP.md

<interfaces>
<!-- 执行者需要了解的现有接口，从源码提取 -->

来自 crates/nova_character/src/attributes.rs：
```rust
pub struct Health {
    pub current: f32,
    pub max: f32,
}
impl Health {
    pub fn new(max: f32) -> Self { Self { current: max, max } }
    pub fn percentage(&self) -> f32 { ... }
}

#[derive(Component, Clone, Debug, Reflect, Serialize, Deserialize)]
pub struct Attributes {
    pub health: Health,
    pub attack: f32,
    pub defense: f32,
    pub move_speed: f32,
    pub attack_range: f32,
    pub attack_speed: f32,
    pub vision_range: f32,
}
impl Default for Attributes { ... }  // health=100, attack=10, defense=5, ...
```

来自 crates/nova_character/src/state.rs：
```rust
#[derive(Component, Clone, Debug, Default, Reflect, PartialEq)]
pub enum CharacterState {
    #[default] Idle,
    Moving { target: Vec3 },
    Attacking { target: Entity },
    Stunned { remaining: f32 },
    Dead,
}
// 现有方法：is_dead(), is_idle(), is_moving(), is_attacking(), is_stunned()
// 缺失方法：current()
```

来自 crates/nova_character/src/prelude.rs：
```rust
pub use crate::attributes::*;
pub use crate::character::*;   // CharacterStats/CharacterBundle 添加后自动导出
pub use crate::state::*;
```

集成测试调用点（来自 tests/integration/character_tests.rs）：
```rust
use nova_character::{CharacterBundle, CharacterState, CharacterStats};
// CharacterBundle::default()
// CharacterStats::new("Test Character").with_health(100.0).with_attack(20.0).with_defense(10.0)
// CharacterBundle { stats, ..default() }
// character_stats.name()        → &str
// character_stats.max_health()  → f32
// character_stats.attack()      → f32
// character_stats.defense()     → f32
// state.current()               → &CharacterState  （用于 matches! 宏）
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>任务 1：在 character.rs 中添加 CharacterStats 和 CharacterBundle</name>
  <files>crates/nova_character/src/character.rs</files>
  <read_first>
    - crates/nova_character/src/character.rs（当前文件全貌，了解已有类型避免命名冲突）
    - crates/nova_character/src/attributes.rs（了解 Attributes 和 Health 结构，CharacterStats 内部持有 Attributes）
    - crates/nova_character/src/state.rs（了解 CharacterState，CharacterBundle 包含此字段）
    - tests/integration/character_tests.rs（了解测试实际调用的 API 签名）
  </read_first>
  <behavior>
    - CharacterStats::new("x") 构造后，name() 返回 "x"
    - CharacterStats::new("x").with_health(100.0).max_health() 返回 100.0
    - CharacterStats::new("x").with_attack(20.0).attack() 返回 20.0
    - CharacterStats::new("x").with_defense(10.0).defense() 返回 10.0
    - CharacterStats::default() 可构造（name 为空字符串，attributes 为 Attributes::default()）
    - CharacterBundle::default() 可构造，四个字段均有 Default
    - CharacterBundle { stats, ..default() } 语法有效（即 Bundle 的 state/transform/visibility 可用 default() 填充）
  </behavior>
  <action>
在文件末尾（Faction 枚举之后）追加以下代码。注意：CharacterStats 持有 Attributes 实例（不是独立组件），通过委托方法暴露数值。

```rust
use crate::attributes::{Attributes, Health};

/// 角色数值统计（Attributes 的 builder 包装）
#[derive(Component, Clone, Debug, Default, Reflect, Serialize, Deserialize)]
pub struct CharacterStats {
    pub name: String,
    pub attributes: Attributes,
}

impl CharacterStats {
    pub fn new(name: &str) -> Self {
        Self {
            name: name.to_string(),
            ..Default::default()
        }
    }

    pub fn with_health(mut self, hp: f32) -> Self {
        self.attributes.health = Health::new(hp);
        self
    }

    pub fn with_attack(mut self, val: f32) -> Self {
        self.attributes.attack = val;
        self
    }

    pub fn with_defense(mut self, val: f32) -> Self {
        self.attributes.defense = val;
        self
    }

    /// 委托访问器（满足集成测试断言）
    pub fn name(&self) -> &str {
        &self.name
    }

    pub fn max_health(&self) -> f32 {
        self.attributes.health.max
    }

    pub fn attack(&self) -> f32 {
        self.attributes.attack
    }

    pub fn defense(&self) -> f32 {
        self.attributes.defense
    }
}

/// 角色完整 ECS Bundle（用于 world.spawn）
#[derive(Bundle, Default)]
pub struct CharacterBundle {
    pub stats: CharacterStats,
    pub state: CharacterState,
    pub transform: Transform,
    pub visibility: Visibility,
}
```

注意事项：
1. CharacterBundle 的 state 字段类型是 CharacterState，需要在文件顶部或通过 use 引入：`use crate::state::CharacterState;`
2. CharacterStats 派生 Reflect + Serialize + Deserialize（与其他 nova_character 类型保持一致）
3. CharacterBundle 只派生 Bundle 和 Default（Bundle 类型不派生 Reflect）
4. 文件顶部已有 `use bevy::prelude::*;`，Transform 和 Visibility 已可用
5. 如果文件顶部没有 `use crate::attributes::...`，需要添加该 use 语句

文件顶部 use 追加（如果缺失）：
```rust
use crate::attributes::{Attributes, Health};
use crate::state::CharacterState;
```
  </action>
  <verify>
    <automated>cargo check -p nova_character 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - character.rs 包含 `pub struct CharacterStats` 定义
    - character.rs 包含 `pub struct CharacterBundle` 定义
    - character.rs 包含 `impl CharacterStats` 块，其中有 `pub fn new`、`pub fn with_health`、`pub fn with_attack`、`pub fn with_defense`、`pub fn name`、`pub fn max_health`、`pub fn attack`、`pub fn defense`
    - character.rs 包含 `#[derive(Bundle, Default)]` 修饰 CharacterBundle
    - `cargo check -p nova_character` 输出无 error（warning 可接受）
  </acceptance_criteria>
  <done>CharacterStats 和 CharacterBundle 已添加，nova_character crate 编译无错误，从 nova_character prelude 可导入两个新类型。</done>
</task>

<task type="auto">
  <name>任务 2：在 state.rs 中为 CharacterState 添加 current() 方法</name>
  <files>crates/nova_character/src/state.rs</files>
  <read_first>
    - crates/nova_character/src/state.rs（当前文件全貌，确认现有 impl 块位置）
    - tests/integration/character_tests.rs（L52：`matches!(state.current(), CharacterState::Idle)` 调用签名）
  </read_first>
  <action>
在 state.rs 的 `impl CharacterState` 块中，在最后一个现有方法（is_stunned）之后追加 current() 方法：

```rust
/// 返回当前状态的引用（用于 matches! 宏模式匹配）
pub fn current(&self) -> &Self {
    self
}
```

这使得 `matches!(state.current(), CharacterState::Idle)` 等价于 `matches!(state, CharacterState::Idle)`，语义正确。

无需修改 derive 属性或其他任何内容。
  </action>
  <verify>
    <automated>cargo check -p nova_character 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - state.rs 包含 `pub fn current(&self) -> &Self`
    - `cargo check -p nova_character` 输出无 error
  </acceptance_criteria>
  <done>CharacterState::current() 方法已添加，返回 &Self，可满足集成测试的模式匹配调用。</done>
</task>

</tasks>

<verification>
执行以下命令验证本 plan 的所有工作：

```bash
cargo check -p nova_character 2>&1
```

期望结果：无编译错误（warning 可接受）。

```bash
grep -n "pub struct CharacterStats\|pub struct CharacterBundle\|pub fn current" \
  /Users/fwind/dosec/AI产品/game_engine/crates/nova_character/src/character.rs \
  /Users/fwind/dosec/AI产品/game_engine/crates/nova_character/src/state.rs
```

期望结果：三处匹配各一行。
</verification>

<success_criteria>
- nova_character crate 编译无错误
- character.rs 包含 CharacterStats（Component + Default + builder API）和 CharacterBundle（Bundle + Default）
- state.rs 包含 CharacterState::current() 方法
- 两个新类型通过 prelude（pub use crate::character::*）自动从 nova_character 导出
</success_criteria>

<output>
完成后在 /Users/fwind/dosec/AI产品/game_engine/.planning/phases/01-测试基础设施修复/ 创建 01-01-SUMMARY.md，记录：
- 修改的文件和核心变更内容
- CharacterStats 的字段布局和访问器签名
- CharacterBundle 的字段列表
- 任何编译警告或需要后续注意的问题
</output>
