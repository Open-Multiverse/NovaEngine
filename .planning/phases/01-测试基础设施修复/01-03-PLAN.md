---
phase: 01-测试基础设施修复
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - crates/nova_test/src/wasm.rs
  - crates/nova_test/tests/integration/character_tests.rs
  - crates/nova_test/tests/integration/ai_tests.rs
autonomous: true
requirements:
  - TESTFIX-04
  - TESTFIX-05
must_haves:
  truths:
    - "BrowserCompatibility::supports_webgpu() 在非 WASM 编译目标下返回 false"
    - "cargo test -p nova_test 编译通过，character_tests 和 ai_tests 中的测试全部通过"
    - "cargo test --all 执行完成，无编译错误，无失败测试"
  artifacts:
    - path: "crates/nova_test/src/wasm.rs"
      provides: "BrowserCompatibility::supports_webgpu() 非 WASM 返回 false"
      contains: "false  // 非 WASM 环境不支持 WebGPU"
    - path: "crates/nova_test/tests/integration/character_tests.rs"
      provides: "character 集成测试挂载在 nova_test crate"
    - path: "crates/nova_test/tests/integration/ai_tests.rs"
      provides: "ai 集成测试挂载在 nova_test crate"
  key_links:
    - from: "nova_test/tests/integration/character_tests.rs"
      to: "nova_character::CharacterBundle"
      via: "use nova_character::{CharacterBundle, CharacterState, CharacterStats}"
      pattern: "use nova_character"
    - from: "nova_test/tests/integration/ai_tests.rs"
      to: "nova_ai::AiAgent"
      via: "use nova_ai::{AiAgent, BehaviorTree, Blackboard}"
      pattern: "use nova_ai"
---

<objective>
完成 Phase 1 的最后两项工作：(1) 修复 BrowserCompatibility::supports_webgpu() 在非 WASM 环境错误返回 true 的 bug，(2) 将 tests/integration/ 下的集成测试文件移入 nova_test crate 的 tests/ 目录使其成为合法的 Cargo 集成测试，最终验证 cargo test --all 全绿。

Purpose: Plan 01 和 02 修复了类型缺失，本 Plan 完成测试挂载和最终验证，实现 Phase 1 目标。
Output: 修复的 wasm.rs，正确挂载的集成测试文件，cargo test --all 全绿
</objective>

<execution_context>
@/Users/fwind/dosec/AI产品/game_engine/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fwind/dosec/AI产品/game_engine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/fwind/dosec/AI产品/game_engine/.planning/PROJECT.md
@/Users/fwind/dosec/AI产品/game_engine/.planning/ROADMAP.md
@/Users/fwind/dosec/AI产品/game_engine/.planning/phases/01-测试基础设施修复/01-01-SUMMARY.md
@/Users/fwind/dosec/AI产品/game_engine/.planning/phases/01-测试基础设施修复/01-02-SUMMARY.md

<interfaces>
<!-- 关键接口信息 -->

nova_test Cargo.toml 依赖（已存在，不需要修改）：
```toml
[dependencies]
nova_character.workspace = true
nova_ai.workspace = true
# ...（其他依赖已存在）
```

nova_test/src/lib.rs 导出（已存在）：
```rust
pub use app_runner::TestApp;
pub use assertions::*;  // assert_has_component! 宏通过此导出
```

tests/integration/character_tests.rs 当前位置（需要移动）：
- 源：/Users/fwind/dosec/AI产品/game_engine/tests/integration/character_tests.rs
- 目标：crates/nova_test/tests/integration/character_tests.rs

tests/integration/ai_tests.rs 当前位置（需要移动）：
- 源：/Users/fwind/dosec/AI产品/game_engine/tests/integration/ai_tests.rs
- 目标：crates/nova_test/tests/integration/ai_tests.rs

当前错误返回值（来自 crates/nova_test/src/wasm.rs:111）：
```rust
#[cfg(not(target_arch = "wasm32"))]
pub fn supports_webgpu() -> bool {
    true  // 错误！应为 false
}
```

集成测试中的宏使用（character_tests.rs:15）：
```rust
assert_has_component!(app, CharacterStats);
// 宏定义在 nova_test::assertions，通过 use nova_test::* 或 #[macro_export] 可访问
// 当前测试文件只有 use nova_test::TestApp，可能需要添加 use nova_test::assert_has_component;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>任务 1：修复 BrowserCompatibility::supports_webgpu() 非 WASM 返回值</name>
  <files>crates/nova_test/src/wasm.rs</files>
  <read_first>
    - crates/nova_test/src/wasm.rs（确认 L110-113 的当前代码，避免修改错误行）
  </read_first>
  <action>
将 crates/nova_test/src/wasm.rs 中的以下代码（约 L109-113）：

```rust
/// 在非 WASM 环境中始终返回 true
#[cfg(not(target_arch = "wasm32"))]
pub fn supports_webgpu() -> bool {
    true
}
```

修改为：

```rust
/// 在非 WASM 环境中始终返回 false（原生环境不支持 WebGPU）
#[cfg(not(target_arch = "wasm32"))]
pub fn supports_webgpu() -> bool {
    false  // 非 WASM 环境不支持 WebGPU
}
```

只修改函数体内的 `true` 为 `false`，以及注释文字。不修改任何其他内容。
  </action>
  <verify>
    <automated>cargo test -p nova_test -- supports_webgpu 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - wasm.rs 中 `#[cfg(not(target_arch = "wasm32"))]` 修饰的 `supports_webgpu` 函数体返回 `false`（不是 `true`）
    - grep 验证：`grep -A3 'cfg(not(target_arch = "wasm32"))' crates/nova_test/src/wasm.rs` 中 `supports_webgpu` 的函数体包含 `false`
    - `cargo check -p nova_test` 无 error
  </acceptance_criteria>
  <done>BrowserCompatibility::supports_webgpu() 在非 WASM 环境返回 false，满足 TESTFIX-05 要求。</done>
</task>

<task type="auto">
  <name>任务 2：将集成测试挂载到 nova_test crate 并验证全套测试通过</name>
  <files>
    crates/nova_test/tests/integration/character_tests.rs
    crates/nova_test/tests/integration/ai_tests.rs
  </files>
  <read_first>
    - tests/integration/character_tests.rs（源文件内容，了解 use 语句和宏调用）
    - tests/integration/ai_tests.rs（源文件内容，了解 use 语句和宏调用）
    - crates/nova_test/src/lib.rs（了解 assert_has_component! 宏的导出路径）
    - crates/nova_test/Cargo.toml（确认 nova_character 和 nova_ai 依赖已存在，无需修改）
  </read_first>
  <action>
**步骤 1：创建目标目录**

创建目录 `crates/nova_test/tests/integration/`（如果不存在）。

**步骤 2：复制 character_tests.rs**

将 `tests/integration/character_tests.rs` 的内容复制到 `crates/nova_test/tests/integration/character_tests.rs`。

在复制后，检查文件头部：
- 如果文件中有 `assert_has_component!(app, ...)` 调用，但没有导入该宏，在文件顶部添加：
  ```rust
  use nova_test::assert_has_component;
  ```
  或者改为：
  ```rust
  use nova_test::*;
  ```
  （注意：`#[macro_export]` 宏在 Rust 2018+ 中可以通过 `use crate_name::macro_name` 导入）

实际上，Rust 2018+ 中 `#[macro_export]` 的宏通过 `use nova_test::assert_has_component` 可访问，但需要显式导入。

最终文件内容应为：
```rust
//! 集成测试 - 角色系统

use nova_character::{CharacterBundle, CharacterState, CharacterStats};
use nova_test::TestApp;
use nova_test::assert_has_component;  // 显式导入宏

#[test]
fn test_character_creation() {
    let mut app = TestApp::new();
    app.world_mut().spawn(CharacterBundle::default());
    app.run_frames(5);
    assert_has_component!(app, CharacterStats);
    assert_has_component!(app, CharacterState);
}

#[test]
fn test_character_stats() {
    let mut app = TestApp::new();
    let stats = CharacterStats::new("Test Character")
        .with_health(100.0)
        .with_attack(20.0)
        .with_defense(10.0);
    app.world_mut().spawn(CharacterBundle { stats, ..default() });
    app.run_frames(3);
    let character_stats = app.world().query::<&CharacterStats>().single(app.world());
    assert_eq!(character_stats.name(), "Test Character");
    assert_eq!(character_stats.max_health(), 100.0);
    assert_eq!(character_stats.attack(), 20.0);
    assert_eq!(character_stats.defense(), 10.0);
}

#[test]
fn test_character_state_transitions() {
    let mut app = TestApp::new();
    let entity = app.world_mut().spawn(CharacterBundle::default()).id();
    app.run_frames(5);
    {
        let state = app.world().get::<CharacterState>(entity).unwrap();
        assert!(matches!(state.current(), CharacterState::Idle));
    }
}
```

**步骤 3：复制 ai_tests.rs**

将 `tests/integration/ai_tests.rs` 的内容复制到 `crates/nova_test/tests/integration/ai_tests.rs`。

同样确保宏导入正确：
```rust
//! 集成测试 - AI 系统

use nova_ai::{AiAgent, BehaviorTree, Blackboard};
use nova_test::TestApp;
use nova_test::assert_has_component;  // 显式导入宏

#[test]
fn test_ai_agent_creation() {
    let mut app = TestApp::new();
    app.world_mut().spawn((
        AiAgent::new(),
        BehaviorTree::default(),
        Blackboard::default(),
    ));
    app.run_frames(5);
    assert_has_component!(app, AiAgent);
    assert_has_component!(app, BehaviorTree);
}

#[test]
fn test_behavior_tree_execution() {
    let mut app = TestApp::new();
    let bt = BehaviorTree::sequence().child(BehaviorTree::action(|blackboard| {
        blackboard.set("test", true);
        true
    }));
    app.world_mut().spawn((AiAgent::new(), bt, Blackboard::default()));
    app.run_frames(10);
    let blackboard = app.world().query::<&Blackboard>().single(app.world());
    assert!(blackboard.get::<bool>("test").unwrap_or(false));
}
```

**步骤 4：验证**

执行 `cargo test -p nova_test` 确认测试通过，然后执行 `cargo test --all` 确认全套通过。

如果 test_behavior_tree_execution 失败（blackboard 未被写入），原因是 behavior_tree_system 未注册到 TestApp 的 App。检查 TestApp::new() 是否添加了 NovaAiPlugin：
- 如果没有，修改 crates/nova_test/src/app_runner.rs 中的 TestApp::new()，添加 `.add_plugins(nova_ai::NovaAiPlugin)`
- 同样检查是否需要 `.add_plugins(nova_character::NovaCharacterPlugin)`
  </action>
  <verify>
    <automated>cargo test --all 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 文件 crates/nova_test/tests/integration/character_tests.rs 存在
    - 文件 crates/nova_test/tests/integration/ai_tests.rs 存在
    - 两个文件顶部包含 `use nova_test::assert_has_component` 或等效导入
    - `cargo test -p nova_test` 输出包含 `test result: ok` 且 `0 failed`
    - `cargo test --all` 输出不包含 `FAILED`，不包含 `error[E` 编译错误
    - `cargo test --all` 输出包含 `test_character_creation ... ok`
    - `cargo test --all` 输出包含 `test_ai_agent_creation ... ok`
    - `cargo test --all` 输出包含 `test_behavior_tree_execution ... ok`
  </acceptance_criteria>
  <done>所有集成测试挂载完成并通过，cargo test --all 全绿，Phase 1 目标达成（TESTFIX-01 到 TESTFIX-05 全部满足）。</done>
</task>

</tasks>

<verification>
执行最终 Phase 1 验收检查：

```bash
cargo test --all 2>&1
```

期望结果：
- 无 `error[E` 编译错误
- 无 `FAILED` 测试
- 含 `test_character_creation ... ok`、`test_character_stats ... ok`、`test_character_state_transitions ... ok`
- 含 `test_ai_agent_creation ... ok`、`test_behavior_tree_execution ... ok`
- BrowserCompatibility::supports_webgpu() 单元测试（若存在）通过

```bash
grep -n "false" /Users/fwind/dosec/AI产品/game_engine/crates/nova_test/src/wasm.rs | grep "supports_webgpu"
```

备用验证（直接检查函数体）：
```bash
grep -A5 'cfg(not(target_arch = "wasm32"))' \
  /Users/fwind/dosec/AI产品/game_engine/crates/nova_test/src/wasm.rs
```
</verification>

<success_criteria>
- BrowserCompatibility::supports_webgpu() 在非 WASM 环境返回 false
- cargo test --all 执行完成，0 错误，0 失败
- character_tests.rs 和 ai_tests.rs 作为 nova_test crate 的集成测试运行
- Phase 1 所有 5 个需求（TESTFIX-01 到 TESTFIX-05）全部验证通过
</success_criteria>

<output>
完成后在 /Users/fwind/dosec/AI产品/game_engine/.planning/phases/01-测试基础设施修复/ 创建 01-03-SUMMARY.md，记录：
- 集成测试挂载方式和最终文件位置
- wasm.rs 修改的具体行和内容
- cargo test --all 的最终输出摘要（通过/失败数量）
- Phase 1 完成状态和所有 TESTFIX 需求的验证结果
</output>
