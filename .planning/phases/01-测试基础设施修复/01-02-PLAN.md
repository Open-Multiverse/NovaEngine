---
phase: 01-测试基础设施修复
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nova_ai/src/blackboard.rs
  - crates/nova_ai/src/behavior.rs
  - crates/nova_ai/src/decision.rs
  - crates/nova_ai/src/lib.rs
  - crates/nova_ai/src/prelude.rs
autonomous: true
requirements:
  - TESTFIX-02
  - TESTFIX-03
must_haves:
  truths:
    - "AiAgent::new() 可构造，实现 Component"
    - "Blackboard::default() 可构造，set/get 泛型方法可用"
    - "BehaviorTree::sequence() 返回根节点为空 Sequence 的树"
    - "BehaviorTree::action(|bb| { bb.set('k', true); true }) 返回 Custom 动作树"
    - "BehaviorTree::sequence().child(BehaviorTree::action(...)) 链式构造有效"
    - "BehaviorTree::default() 等价于 BehaviorTree::sequence()"
    - "behavior_tree_system 可处理 Custom 动作节点并写入 Blackboard"
  artifacts:
    - path: "crates/nova_ai/src/blackboard.rs"
      provides: "Blackboard 组件"
      exports:
        - "pub struct Blackboard"
    - path: "crates/nova_ai/src/behavior.rs"
      provides: "ActionNode::Custom 变体 + BehaviorTree builder API + Default impl"
      contains: "Custom(Arc<dyn Fn"
    - path: "crates/nova_ai/src/decision.rs"
      provides: "AiAgent 组件 + behavior_tree_system 支持 Custom 动作执行"
      exports:
        - "pub struct AiAgent"
  key_links:
    - from: "behavior.rs ActionNode::Custom"
      to: "blackboard.rs Blackboard"
      via: "use crate::blackboard::Blackboard"
      pattern: "use crate::blackboard::Blackboard"
    - from: "decision.rs behavior_tree_system"
      to: "Blackboard"
      via: "Option<&mut Blackboard> in Query"
      pattern: "Option<&mut Blackboard>"
---

<objective>
在 nova_ai crate 中补齐三个缺失内容：(1) 新建 blackboard.rs 定义 Blackboard 组件，(2) 为 behavior.rs 中的 ActionNode 添加 Custom 变体并为 BehaviorTree 添加 sequence/action/child/Default，(3) 在 decision.rs 中添加 AiAgent 组件并更新 behavior_tree_system 以支持 Custom 动作执行。

Purpose: 集成测试 ai_tests.rs 引用了这三个不存在的 API。完成后 Wave 2 的测试挂载计划可以运行 ai 相关测试。
Output: 新增的 blackboard.rs，修改后的 behavior.rs 和 decision.rs，更新后的 lib.rs 和 prelude.rs
</objective>

<execution_context>
@/Users/fwind/dosec/AI产品/game_engine/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fwind/dosec/AI产品/game_engine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/fwind/dosec/AI产品/game_engine/.planning/PROJECT.md
@/Users/fwind/dosec/AI产品/game_engine/.planning/ROADMAP.md

<interfaces>
<!-- 从源码提取的现有接口，供执行者直接使用 -->

来自 crates/nova_ai/src/behavior.rs（当前状态）：
```rust
#[derive(Component, Clone, Debug)]
pub struct BehaviorTree {
    pub root: BehaviorNode,
}
impl BehaviorTree {
    pub fn new(root: BehaviorNode) -> Self { ... }
    pub fn standard_soldier() -> Self { ... }   // 不修改
    pub fn coward() -> Self { ... }             // 不修改
}

// ActionNode 当前变体（需要添加 Custom）：
#[derive(Clone, Debug)]
pub enum ActionNode {
    Idle,
    MoveTo(MoveTarget),
    Attack(AttackTarget),
    Flee,
    Patrol { points: Vec<Vec3>, current: usize },
    FollowLeader,
}

// BehaviorNode 枚举（不修改结构，只受 ActionNode 变化影响）
#[derive(Clone, Debug)]
pub enum BehaviorNode { ... }
```

来自 crates/nova_ai/src/decision.rs（当前状态）：
```rust
// 当前 behavior_tree_system Query（需要加入 Blackboard）：
pub fn behavior_tree_system(
    mut query: Query<(
        Entity,
        &Transform,
        &Attributes,
        &PerceivedEntities,
        Option<&Emotion>,
        &BehaviorTree,
        // 缺少 Option<&mut Blackboard>
    )>,
    mut commands: Commands,
)
// 当前 execute_action 签名（需要添加 blackboard 参数）：
fn execute_action(action: &ActionNode, ctx: &BtContext, commands: &mut EntityCommands) -> BehaviorStatus
```

集成测试调用点（来自 tests/integration/ai_tests.rs）：
```rust
use nova_ai::{AiAgent, BehaviorTree, Blackboard};
// AiAgent::new()
// BehaviorTree::default()
// Blackboard::default()
// Blackboard::set::<bool>("key", true)
// Blackboard::get::<bool>("key") -> Option<&bool>
// BehaviorTree::sequence().child(BehaviorTree::action(|blackboard| {
//     blackboard.set("test", true);
//     true
// }))
// app.run_frames(10) 后 blackboard.get::<bool>("test").unwrap_or(false) 为 true
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>任务 1：新建 blackboard.rs，定义 Blackboard 组件</name>
  <files>crates/nova_ai/src/blackboard.rs</files>
  <read_first>
    - crates/nova_ai/src/lib.rs（了解模块结构，确认新模块的注册位置）
    - tests/integration/ai_tests.rs（了解测试对 Blackboard API 的实际调用）
  </read_first>
  <behavior>
    - Blackboard::default() 可构造，data 字段为空 HashMap
    - blackboard.set("key", true_value) 后 blackboard.get::<bool>("key") 返回 Some(&true)
    - blackboard.get::<bool>("missing") 返回 None
    - blackboard.get::<i32>("key_set_as_bool") 返回 None（类型不匹配时安全返回 None）
    - Blackboard 实现 Component，可挂载到 Bevy 实体
  </behavior>
  <action>
创建新文件 crates/nova_ai/src/blackboard.rs，内容如下：

```rust
//! AI 黑板组件——用于行为树节点之间共享运行时状态

use std::{any::Any, collections::HashMap};
use bevy::prelude::*;

/// AI 黑板：键值存储，用于行为树节点读写运行时状态
///
/// 使用类型擦除（Box<dyn Any>）存储任意 Send + Sync 值。
/// 不实现 Clone（通过 Query<&mut Blackboard> 访问，无需克隆）。
#[derive(Component, Default)]
pub struct Blackboard {
    data: HashMap<String, Box<dyn Any + Send + Sync>>,
}

impl Blackboard {
    /// 写入任意类型的值
    pub fn set<T: Any + Send + Sync>(&mut self, key: &str, value: T) {
        self.data.insert(key.to_string(), Box::new(value));
    }

    /// 读取指定类型的值，类型不匹配时返回 None
    pub fn get<T: Any + 'static>(&self, key: &str) -> Option<&T> {
        self.data.get(key)?.downcast_ref()
    }

    /// 删除指定键
    pub fn remove(&mut self, key: &str) {
        self.data.remove(key);
    }

    /// 检查键是否存在
    pub fn contains(&self, key: &str) -> bool {
        self.data.contains_key(key)
    }
}
```

注意：Blackboard 不实现 Debug（因为 Box<dyn Any> 不实现 Debug）。如果 Bevy 某些 derive 宏要求 Debug，可以手动实现一个简单的 Debug：
```rust
impl std::fmt::Debug for Blackboard {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "Blackboard {{ {} keys }}", self.data.len())
    }
}
```

然后，在 crates/nova_ai/src/lib.rs 中添加模块声明（在 `pub mod behavior;` 之后）：
```rust
pub mod blackboard;
```

在 crates/nova_ai/src/prelude.rs 中添加导出（在现有 `pub use crate::behavior::*;` 之前）：
```rust
pub use crate::blackboard::*;
```
  </action>
  <verify>
    <automated>cargo check -p nova_ai 2>&1 | grep "^error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - 文件 crates/nova_ai/src/blackboard.rs 存在
    - blackboard.rs 包含 `pub struct Blackboard`
    - blackboard.rs 包含 `pub fn set<T: Any + Send + Sync>`
    - blackboard.rs 包含 `pub fn get<T: Any + 'static>`
    - lib.rs 包含 `pub mod blackboard`
    - prelude.rs 包含 `pub use crate::blackboard::*`
    - `cargo check -p nova_ai` 无新增 error
  </acceptance_criteria>
  <done>Blackboard 组件已创建，nova_ai 模块系统已更新，nova_ai::Blackboard 可导入和使用。</done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：在 behavior.rs 中添加 ActionNode::Custom 变体和 BehaviorTree builder API</name>
  <files>crates/nova_ai/src/behavior.rs</files>
  <read_first>
    - crates/nova_ai/src/behavior.rs（当前文件全貌，确认 ActionNode 和 BehaviorTree 的 derive 宏及现有方法）
    - crates/nova_ai/src/blackboard.rs（刚创建的文件，确认 Blackboard 类型路径）
    - tests/integration/ai_tests.rs（了解 builder API 的实际调用链）
  </read_first>
  <behavior>
    - BehaviorTree::sequence() 构造根节点为 BehaviorNode::Sequence(vec![]) 的树
    - BehaviorTree::action(|bb| { bb.set("k", true); true }) 构造含 ActionNode::Custom 的树
    - BehaviorTree::sequence().child(BehaviorTree::action(f)) 将子节点追加到 Sequence 中
    - BehaviorTree::default() 等价于 BehaviorTree::sequence()（空 Sequence）
    - ActionNode::Custom 可 Clone（Arc 的 clone 只增加引用计数）
    - ActionNode::Custom 的 Debug 输出为 "Custom(fn)"（不会 panic 或编译失败）
  </behavior>
  <action>
修改 crates/nova_ai/src/behavior.rs，进行以下三处变更：

**变更 1：在文件顶部添加 use 语句**

在 `use bevy::prelude::*;` 之后添加：
```rust
use std::sync::Arc;
use crate::blackboard::Blackboard;
```

**变更 2：修改 ActionNode 枚举**

将：
```rust
/// 行为动作节点
#[derive(Clone, Debug)]
pub enum ActionNode {
    Idle,
    MoveTo(MoveTarget),
    Attack(AttackTarget),
    Flee,
    Patrol { points: Vec<Vec3>, current: usize },
    FollowLeader,
}
```

替换为：
```rust
/// 行为动作节点
#[derive(Clone)]
pub enum ActionNode {
    Idle,
    MoveTo(MoveTarget),
    Attack(AttackTarget),
    Flee,
    Patrol { points: Vec<Vec3>, current: usize },
    FollowLeader,
    /// 自定义闭包动作，接收可变 Blackboard 引用，返回 bool（true=成功，false=失败）
    /// 使用 Arc 使其可 Clone（引用计数，低成本）
    Custom(Arc<dyn Fn(&mut Blackboard) -> bool + Send + Sync>),
}

impl std::fmt::Debug for ActionNode {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            ActionNode::Idle => write!(f, "ActionNode::Idle"),
            ActionNode::MoveTo(t) => write!(f, "ActionNode::MoveTo({:?})", t),
            ActionNode::Attack(t) => write!(f, "ActionNode::Attack({:?})", t),
            ActionNode::Flee => write!(f, "ActionNode::Flee"),
            ActionNode::Patrol { points, current } => {
                write!(f, "ActionNode::Patrol {{ points: {:?}, current: {} }}", points, current)
            }
            ActionNode::FollowLeader => write!(f, "ActionNode::FollowLeader"),
            ActionNode::Custom(_) => write!(f, "ActionNode::Custom(fn)"),
        }
    }
}
```

注意：BehaviorNode 派生 `#[derive(Clone, Debug)]`，因为它包含 ActionNode，所以 BehaviorNode 的 Clone 和 Debug 也受影响。由于 ActionNode 已改为手动实现 Debug，BehaviorNode 的 `#[derive(Debug)]` 会通过委托 ActionNode 的手动 Debug 实现，这是正确的（Rust derive 会调用字段的 Debug 实现）。

**变更 3：在 BehaviorTree impl 块末尾添加 builder 方法和 Default impl**

在现有 `impl BehaviorTree` 块中，在 `coward()` 方法之后添加：

```rust
    /// 创建空顺序节点（用作 builder 起点）
    pub fn sequence() -> Self {
        Self { root: BehaviorNode::Sequence(vec![]) }
    }

    /// 创建自定义动作节点，接受返回 bool 的闭包
    pub fn action(f: impl Fn(&mut Blackboard) -> bool + Send + Sync + 'static) -> Self {
        Self { root: BehaviorNode::Action(ActionNode::Custom(Arc::new(f))) }
    }

    /// 链式添加子节点（仅对 Sequence/Selector/Parallel 有效，其他节点类型静默忽略）
    pub fn child(mut self, child: BehaviorTree) -> Self {
        match &mut self.root {
            BehaviorNode::Sequence(children)
            | BehaviorNode::Selector(children)
            | BehaviorNode::Parallel(children) => {
                children.push(child.root);
            }
            _ => {}
        }
        self
    }
```

然后在 `impl BehaviorTree` 块之后添加 Default 实现：
```rust
impl Default for BehaviorTree {
    fn default() -> Self {
        Self::sequence()
    }
}
```
  </action>
  <verify>
    <automated>cargo check -p nova_ai 2>&1 | grep "^error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - behavior.rs 包含 `Custom(Arc<dyn Fn(&mut Blackboard) -> bool + Send + Sync>)`
    - behavior.rs 包含 `impl std::fmt::Debug for ActionNode`
    - behavior.rs 包含 `pub fn sequence() -> Self`
    - behavior.rs 包含 `pub fn action(`
    - behavior.rs 包含 `pub fn child(mut self, child: BehaviorTree) -> Self`
    - behavior.rs 包含 `impl Default for BehaviorTree`
    - `cargo check -p nova_ai` 无 error（ActionNode::Custom 相关的 Clone/Debug 编译通过）
  </acceptance_criteria>
  <done>ActionNode::Custom 变体已添加，BehaviorTree 具备 sequence/action/child builder 方法和 Default 实现，nova_ai crate 编译无错误。</done>
</task>

<task type="auto" tdd="true">
  <name>任务 3：在 decision.rs 中添加 AiAgent 并更新 behavior_tree_system 支持 Custom 动作</name>
  <files>crates/nova_ai/src/decision.rs</files>
  <read_first>
    - crates/nova_ai/src/decision.rs（当前文件全貌，了解 BtContext、evaluate_node、execute_action 的完整签名）
    - crates/nova_ai/src/blackboard.rs（了解 Blackboard 的 set/get 方法签名）
    - crates/nova_ai/src/behavior.rs（了解 ActionNode::Custom 的类型签名）
    - tests/integration/ai_tests.rs（了解测试的期望：run_frames(10) 后 blackboard.get::<bool>("test") 为 true）
  </read_first>
  <behavior>
    - AiAgent::new() 构造 AiAgent 标记组件
    - AiAgent 实现 Component 和 Default
    - behavior_tree_system 遇到 ActionNode::Custom(f) 时调用 f(&mut blackboard)，返回值为 true 时 BehaviorStatus::Success，false 时 BehaviorStatus::Failure
    - 当实体没有 Blackboard 组件时，Custom 动作返回 BehaviorStatus::Failure（安全降级）
    - 行为树每帧执行，集成测试 run_frames(10) 后 blackboard 中 "test" 键值为 true
  </behavior>
  <action>
修改 crates/nova_ai/src/decision.rs，进行以下变更：

**变更 1：添加 AiAgent 组件**

在文件顶部 use 块之后，在 `BtContext` 定义之前，添加：
```rust
use crate::blackboard::Blackboard;

/// AI 控制标记组件——挂载此组件的实体受行为树系统驱动
#[derive(Component, Default, Clone, Debug)]
pub struct AiAgent;

impl AiAgent {
    pub fn new() -> Self {
        Self
    }
}
```

**变更 2：更新 BtContext 添加 blackboard 字段**

将：
```rust
pub struct BtContext<'a> {
    pub entity: Entity,
    pub transform: &'a Transform,
    pub attributes: &'a Attributes,
    pub perceived: &'a PerceivedEntities,
    pub emotion: Option<&'a Emotion>,
}
```

替换为：
```rust
pub struct BtContext<'a> {
    pub entity: Entity,
    pub transform: &'a Transform,
    pub attributes: &'a Attributes,
    pub perceived: &'a PerceivedEntities,
    pub emotion: Option<&'a Emotion>,
    pub blackboard: Option<*mut Blackboard>,  // 原始指针避免借用检查器冲突
}
```

注意：使用原始指针是因为 `evaluate_node` 递归调用时 `BtContext` 是只读引用，但 `Custom` 动作需要可变 `Blackboard`。替代方案是使用 `RefCell<&mut Blackboard>`，但原始指针更简洁。如果项目有安全策略限制，改用下面的替代方案 B。

**替代方案 B（更安全，推荐）：** 不修改 BtContext，改为在 execute_action 中单独传入可变 Blackboard 引用。

将 execute_action 签名改为：
```rust
fn execute_action(
    action: &ActionNode,
    ctx: &BtContext,
    commands: &mut EntityCommands,
    blackboard: &mut Option<Mut<Blackboard>>,
) -> BehaviorStatus
```

将 evaluate_node 签名改为：
```rust
pub fn evaluate_node(
    node: &BehaviorNode,
    ctx: &BtContext,
    commands: &mut EntityCommands,
    blackboard: &mut Option<Mut<Blackboard>>,
) -> BehaviorStatus
```

并在所有递归调用处传递 `blackboard`。

**使用替代方案 B**（避免 unsafe），完整修改如下：

1. 将所有 `evaluate_node` 调用签名改为接收 `blackboard: &mut Option<Mut<Blackboard>>`
2. 在 `execute_action` 的 `ActionNode::Custom(f)` 分支中：
   ```rust
   ActionNode::Custom(f) => {
       if let Some(bb) = blackboard.as_deref_mut() {
           if f(bb) {
               BehaviorStatus::Success
           } else {
               BehaviorStatus::Failure
           }
       } else {
           BehaviorStatus::Failure
       }
   }
   ```
3. 更新 `behavior_tree_system` 的 Query 和迭代逻辑：
   ```rust
   pub fn behavior_tree_system(
       mut query: Query<(
           Entity,
           &Transform,
           &Attributes,
           &PerceivedEntities,
           Option<&Emotion>,
           &BehaviorTree,
           Option<&mut Blackboard>,
       )>,
       mut commands: Commands,
   ) {
       for (entity, transform, attributes, perceived, emotion, tree, mut blackboard) in query.iter_mut() {
           let ctx = BtContext {
               entity,
               transform,
               attributes,
               perceived,
               emotion,
           };
           let root = tree.root.clone();
           let mut entity_commands = commands.entity(entity);
           evaluate_node(&root, &ctx, &mut entity_commands, &mut blackboard);
       }
   }
   ```

完整修改说明：
- BtContext 结构体不变（不添加 blackboard 字段），保持简洁
- evaluate_node 的公开签名需要添加 `blackboard` 参数
- evaluate_node 内部所有递归调用也需要传递 `blackboard`
- execute_action 新增 `blackboard` 参数，并处理 `ActionNode::Custom` 分支
- behavior_tree_system 的 Query 添加 `Option<&mut Blackboard>`，iter() 改为 iter_mut()
- `Mut<Blackboard>` 是 Bevy 的智能指针，`as_deref_mut()` 返回 `Option<&mut Blackboard>`
  </action>
  <verify>
    <automated>cargo check -p nova_ai 2>&1 | grep "^error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - decision.rs 包含 `pub struct AiAgent`
    - decision.rs 包含 `impl AiAgent` 块，其中有 `pub fn new`
    - decision.rs 包含 `ActionNode::Custom(f)` 分支的处理逻辑
    - decision.rs 的 `behavior_tree_system` Query 包含 `Option<&mut Blackboard>`
    - decision.rs 的 `behavior_tree_system` 使用 `iter_mut()` 而非 `iter()`
    - `cargo check -p nova_ai` 无 error
  </acceptance_criteria>
  <done>AiAgent 组件已添加，behavior_tree_system 已更新支持 Custom 动作执行并写入 Blackboard，nova_ai crate 编译无错误。</done>
</task>

</tasks>

<verification>
执行以下命令验证本 plan 的所有工作：

```bash
cargo check -p nova_ai 2>&1 | grep "^error"
```

期望结果：无输出（无编译错误）。

```bash
grep -rn "pub struct AiAgent\|pub struct Blackboard\|pub fn sequence\|pub fn action\|Custom(Arc" \
  /Users/fwind/dosec/AI产品/game_engine/crates/nova_ai/src/
```

期望结果：至少 5 处匹配。
</verification>

<success_criteria>
- nova_ai crate 编译无错误
- nova_ai::AiAgent、nova_ai::Blackboard、nova_ai::BehaviorTree 可从外部 crate 导入
- BehaviorTree::sequence().child(BehaviorTree::action(f)) 链式调用可编译
- BehaviorTree::default() 可调用
- behavior_tree_system 处理 Custom 动作时写入 Blackboard
</success_criteria>

<output>
完成后在 /Users/fwind/dosec/AI产品/game_engine/.planning/phases/01-测试基础设施修复/ 创建 01-02-SUMMARY.md，记录：
- 新建/修改的文件及核心变更
- Blackboard 的 set/get 泛型签名
- AiAgent 的定义（标记组件）
- ActionNode::Custom 的类型签名
- BehaviorTree builder API 的方法签名
- behavior_tree_system 的 Query 变更
- 任何需要后续关注的编译警告
</output>
