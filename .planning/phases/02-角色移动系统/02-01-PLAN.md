---
phase: 02-角色移动系统
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nova_character/Cargo.toml
  - crates/nova_character/src/movement.rs
  - crates/nova_character/src/character.rs
  - crates/nova_character/src/lib.rs
autonomous: true
requirements:
  - MOVE-01
  - MOVE-02
  - MOVE-03
  - MOVE-04

must_haves:
  truths:
    - "带 CharacterState::Moving { target } 的实体，其 Transform 每帧向 target 靠近"
    - "单位到达目标（距离 <= 0.5）后，CharacterState 自动切换为 Idle"
    - "移动速度来自 Attributes.move_speed（默认 5.0），修改该值直接影响移动速度"
    - "有 TileMap Resource 时，单位沿 A* 路径点移动；无 TileMap 时静默降级为直线移动"
  artifacts:
    - path: "crates/nova_character/src/movement.rs"
      provides: "PreviousCharacterState 组件 + update_previous_state_system + movement_system"
      exports:
        - "PreviousCharacterState"
        - "update_previous_state_system"
        - "movement_system"
    - path: "crates/nova_character/src/character.rs"
      provides: "CharacterBundle 含 PreviousCharacterState 字段"
      contains: "prev_state: PreviousCharacterState"
    - path: "crates/nova_character/src/lib.rs"
      provides: "NovaCharacterPlugin 注册两个新系统"
      contains: "update_previous_state_system"
    - path: "crates/nova_character/Cargo.toml"
      provides: "nova_map 依赖"
      contains: "nova_map"
  key_links:
    - from: "CharacterState::Moving { target }"
      to: "Transform.translation"
      via: "movement_system 每帧更新"
      pattern: "transform\\.translation \\+="
    - from: "movement_system"
      to: "Pathfinder::find_path"
      via: "首次进入 Moving 时调用"
      pattern: "Pathfinder::find_path"
    - from: "PathFollow::finished == true"
      to: "CharacterState::Idle"
      via: "movement_system 检测 finished 后 insert Idle"
      pattern: "CharacterState::Idle"
---

<objective>
实现 `movement.rs`，使 `CharacterState::Moving { target }` 状态真正驱动单位 Transform 每帧移动。包含 A* 寻路触发（首次进入 Moving 状态时）、PathFollow 路径点跟随、到达判定与 Idle 状态切换、无 TileMap 时直线降级。

Purpose: Phase 2 的核心目标——AI 单位从静止变为可移动。`movement_system` 是 `behavior_tree_system` 写入 `CharacterState::Moving` 后的响应侧，实现后 AI 单位将真正在场景中移动。

Output: `crates/nova_character/src/movement.rs`（新建）、`Cargo.toml` 新增 `nova_map` 依赖、`CharacterBundle` 新增 `PreviousCharacterState` 字段、`NovaCharacterPlugin` 注册两个新系统。
</objective>

<execution_context>
@/Users/fwind/dosec/AI产品/game_engine/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fwind/dosec/AI产品/game_engine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-角色移动系统/02-CONTEXT.md
@.planning/phases/02-角色移动系统/02-RESEARCH.md

<interfaces>
<!-- 执行器必须在实现前阅读以下接口，不需要额外探索代码库 -->

来自 crates/nova_character/src/state.rs:
```rust
#[derive(Component, Clone, Debug, Default, Reflect, PartialEq)]
pub enum CharacterState {
    #[default]
    Idle,
    Moving { target: Vec3 },
    Attacking { target: Entity },
    Stunned { remaining: f32 },
    Dead,
}
impl CharacterState {
    pub fn is_moving(&self) -> bool { matches!(self, Self::Moving { .. }) }
    pub fn is_idle(&self) -> bool { matches!(self, Self::Idle) }
    // ...
}
// stun_tick_system 模式（直接 mut Query 赋值）：
pub fn stun_tick_system(time: Res<Time>, mut query: Query<&mut CharacterState>) {
    for mut state in query.iter_mut() {
        if let CharacterState::Stunned { remaining } = state.as_mut() {
            *remaining -= time.delta_secs();
            if *remaining <= 0.0 { *state = CharacterState::Idle; }
        }
    }
}
```

来自 crates/nova_character/src/attributes.rs:
```rust
#[derive(Component, Clone, Debug, Reflect, Serialize, Deserialize)]
pub struct Attributes {
    pub health: Health,
    pub attack: f32,
    pub defense: f32,
    pub move_speed: f32,   // 默认 5.0
    pub attack_range: f32,
    pub attack_speed: f32,
    pub vision_range: f32,
}
impl Default for Attributes { /* move_speed: 5.0 */ }
```

来自 crates/nova_character/src/character.rs（当前 CharacterBundle，需要修改）：
```rust
#[derive(Bundle, Default)]
pub struct CharacterBundle {
    pub stats: CharacterStats,
    pub state: CharacterState,
    pub transform: Transform,
    pub visibility: Visibility,
    // 需要新增：pub prev_state: PreviousCharacterState,
    // 需要新增：pub attributes: Attributes,   ← 检查是否已有，若无则补充
}
```

来自 crates/nova_map/src/pathfinding.rs:
```rust
pub struct PathResult { pub path: Vec<(u32, u32)>, pub cost: f32 }

pub struct Pathfinder;
impl Pathfinder {
    // start == goal 时返回 Some(PathResult { path: vec![], cost: 0.0 })
    // 目标不可达时返回 None
    pub fn find_path(tilemap: &TileMap, start: (u32, u32), goal: (u32, u32)) -> Option<PathResult>
}

#[derive(Component, Clone, Debug, Reflect)]
pub struct PathFollow {
    pub path: Vec<(u32, u32)>,
    pub current_index: usize,
    pub finished: bool,
}
impl PathFollow {
    pub fn new(path: Vec<(u32, u32)>) -> Self
    pub fn current_target(&self) -> Option<(u32, u32)>  // 返回当前目标瓦片
    pub fn advance(&mut self)  // 推进索引；超出范围时 finished = true
}
```

来自 crates/nova_map/src/tilemap.rs:
```rust
#[derive(Resource, Clone, Debug, Serialize, Deserialize, Reflect)]
pub struct TileMap { /* ... */ }
impl TileMap {
    pub fn new(width: u32, height: u32, tile_size: f32) -> Self
    pub fn world_to_tile(&self, world_pos: Vec3) -> Option<(u32, u32)>
    pub fn tile_to_world(&self, x: u32, y: u32) -> Vec3  // 返回瓦片中心世界坐标
}
```

来自 crates/nova_formation/src/movement.rs（参考复用的移动模式）：
```rust
// 标准移动插值：防止超越目标（step.min(distance)）
let diff = target_pos - member_transform.translation;
let distance = diff.length();
if distance > 0.5 {
    let speed = 5.0_f32;  // ← 改为 attrs.move_speed
    let step = speed * time.delta_secs();
    member_transform.translation += diff.normalize() * step.min(distance);
}
```

来自 crates/nova_character/src/lib.rs（当前插件，需要修改）：
```rust
pub struct NovaCharacterPlugin;
impl Plugin for NovaCharacterPlugin {
    fn build(&self, app: &mut App) {
        use crate::{attributes::Attributes, state::*};
        app.register_type::<CharacterState>()
           // ... register_type 调用
           .add_systems(Update, (
               state::stun_tick_system,
               feedback::damage_number_system,
               feedback::hit_flash_system,
           ));
    }
}
// 需要新增到 add_systems：
// movement::update_previous_state_system,
// movement::movement_system.after(movement::update_previous_state_system),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>任务 1：新增 nova_map 依赖 + 创建 movement.rs（含两个系统）</name>
  <files>
    crates/nova_character/Cargo.toml
    crates/nova_character/src/movement.rs
  </files>
  <read_first>
    - crates/nova_character/Cargo.toml（当前依赖列表，确认缺少 nova_map）
    - crates/nova_map/src/pathfinding.rs（PathFollow / Pathfinder API 全文）
    - crates/nova_map/src/tilemap.rs（TileMap::world_to_tile / tile_to_world 签名）
    - crates/nova_character/src/state.rs（CharacterState 枚举，stun_tick_system 模式）
    - crates/nova_character/src/attributes.rs（Attributes.move_speed 字段）
    - crates/nova_formation/src/movement.rs（复用 diff.normalize() * step.min(distance) 模式）
    - .planning/phases/02-角色移动系统/02-RESEARCH.md（陷阱列表、代码示例）
  </read_first>
  <action>
**步骤 1：编辑 nova_character/Cargo.toml**

在 `[dependencies]` 区块末尾新增一行：
```toml
nova_map = { workspace = true }
```

**步骤 2：新建 crates/nova_character/src/movement.rs**

文件完整内容如下（严格遵循 CONTEXT.md 所有决策）：

```rust
//! 角色移动系统
//!
//! 实现 CharacterState::Moving { target } 驱动的 Transform 更新，
//! 集成 A* 寻路（PathFollow）和直线降级（无 TileMap 时）。

use bevy::prelude::*;
use nova_map::{
    pathfinding::{PathFollow, Pathfinder},
    tilemap::TileMap,
};

use crate::{attributes::Attributes, state::CharacterState};

/// 记录上一帧是否处于 Moving 状态
///
/// 使用 bool newtype 避免克隆含 Entity/Vec3 字段的 CharacterState 枚举。
/// 必须随 CharacterBundle 一起 spawn（默认 was_moving: false）。
#[derive(Component, Debug, Default, Reflect)]
pub struct PreviousCharacterState {
    pub was_moving: bool,
}

/// 状态历史更新系统（必须在 movement_system 之前运行）
///
/// 将本帧 CharacterState 的 is_moving() 写入 PreviousCharacterState，
/// 供下一步 movement_system 判断是否"首次进入 Moving"。
pub fn update_previous_state_system(
    mut query: Query<(&CharacterState, &mut PreviousCharacterState)>,
) {
    for (state, mut prev) in query.iter_mut() {
        prev.was_moving = state.is_moving();
    }
}

/// 角色移动系统
///
/// 逻辑流：
/// 1. 只处理 CharacterState::Moving { target } 的实体
/// 2. 若上一帧不是 Moving（首次进入）→ 触发 A* 寻路，结果写入 PathFollow
///    - 有 TileMap：调用 Pathfinder::find_path；路径为空则直接切换 Idle
///    - 无 TileMap：静默降级，直接线性移动到 target（不插入 PathFollow）
///    - world_to_tile 返回 None 或 find_path 返回 None：同样降级直线移动
/// 3. 有 PathFollow 组件（路径跟随模式）：
///    - current_target() → tile_to_world → 计算 diff/distance
///    - distance > 0.5：更新 Transform.translation
///    - distance <= 0.5：调用 advance()
///    - finished == true：insert Idle，commands.remove::<PathFollow>（下帧生效）
/// 4. 无 PathFollow（直线降级模式）：
///    - 从 CharacterState::Moving { target } 读取 target
///    - 标准 diff/distance 移动
///    - distance <= 0.5：直接 *state = CharacterState::Idle
pub fn movement_system(
    time: Res<Time>,
    tilemap: Option<Res<TileMap>>,
    mut commands: Commands,
    mut query: Query<(
        Entity,
        &mut Transform,
        &mut CharacterState,
        &Attributes,
        Option<&mut PathFollow>,
        Option<&PreviousCharacterState>,
    )>,
) {
    for (entity, mut transform, mut state, attrs, path_follow, prev_state) in query.iter_mut() {
        // 只处理 Moving 状态
        let target = match *state {
            CharacterState::Moving { target } => target,
            _ => continue,
        };

        // 判断是否首次进入 Moving（上一帧不是 Moving）
        let was_moving = prev_state.map(|p| p.was_moving).unwrap_or(false);

        if !was_moving && path_follow.is_none() {
            // 首次进入 Moving —— 触发一次寻路
            if let Some(ref tm) = tilemap {
                if let (Some(start_tile), Some(goal_tile)) = (
                    tm.world_to_tile(transform.translation),
                    tm.world_to_tile(target),
                ) {
                    match Pathfinder::find_path(tm, start_tile, goal_tile) {
                        Some(result) if !result.path.is_empty() => {
                            commands.entity(entity).insert(PathFollow::new(result.path));
                        }
                        Some(_) => {
                            // start == goal，路径为空，直接 Idle
                            *state = CharacterState::Idle;
                        }
                        None => {
                            // 目标不可达，降级直线移动（不插入 PathFollow）
                        }
                    }
                }
                // world_to_tile 返回 None（超出地图范围），降级直线移动
            }
            // 无 TileMap：静默降级，直接进入直线移动分支
        }

        // 路径跟随模式（有 PathFollow 组件）
        if let Some(mut pf) = path_follow {
            if pf.finished {
                // PathFollow 已完成（Commands 延迟删除前的守卫）
                continue;
            }

            if let Some(tile) = pf.current_target() {
                let target_world = tilemap
                    .as_deref()
                    .map(|tm| tm.tile_to_world(tile.0, tile.1))
                    .unwrap_or(Vec3::new(tile.0 as f32, 0.0, tile.1 as f32));

                let diff = target_world - transform.translation;
                let distance = diff.length();

                if distance > 0.5 {
                    let step = attrs.move_speed * time.delta_secs();
                    transform.translation += diff.normalize() * step.min(distance);
                } else {
                    pf.advance();
                    if pf.finished {
                        *state = CharacterState::Idle;
                        commands.entity(entity).remove::<PathFollow>();
                    }
                }
            }
        } else {
            // 直线降级模式（无 PathFollow）
            let diff = target - transform.translation;
            let distance = diff.length();

            if distance > 0.5 {
                let step = attrs.move_speed * time.delta_secs();
                transform.translation += diff.normalize() * step.min(distance);
            } else {
                *state = CharacterState::Idle;
            }
        }
    }
}
```

**注意事项：**
- `nova_map` 的导入路径需确认 crate 名称（检查 nova_map/Cargo.toml 的 `name` 字段）
- `Option<Res<TileMap>>` 用 `tilemap.as_deref()` 获取 `&TileMap` 引用
- 无 TileMap 时不打任何日志（CONTEXT.md 明确要求静默降级）
- 路径点跟随中 `tile_to_world` 需要 `&TileMap`；降级时用简单坐标转换
  </action>
  <verify>
    <automated>cargo check -p nova_character 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - crates/nova_character/Cargo.toml 包含 `nova_map = { workspace = true }`
    - crates/nova_character/src/movement.rs 存在且包含 `pub struct PreviousCharacterState`
    - movement.rs 包含 `pub fn update_previous_state_system`
    - movement.rs 包含 `pub fn movement_system`
    - movement.rs 包含 `Pathfinder::find_path`（寻路调用）
    - movement.rs 包含 `attrs.move_speed`（从 Attributes 读取速度）
    - movement.rs 包含 `diff.normalize() * step.min(distance)`（移动插值模式）
    - movement.rs 不包含任何 `warn!` 或 `info!` 宏（无 TileMap 时静默）
    - `cargo check -p nova_character` 无编译错误
  </acceptance_criteria>
  <done>nova_character 编译通过，movement.rs 包含两个系统和 PreviousCharacterState 组件</done>
</task>

<task type="auto">
  <name>任务 2：更新 CharacterBundle（加入 PreviousCharacterState + Attributes）并注册系统到 NovaCharacterPlugin</name>
  <files>
    crates/nova_character/src/character.rs
    crates/nova_character/src/lib.rs
  </files>
  <read_first>
    - crates/nova_character/src/character.rs（CharacterBundle 当前字段，确认是否已有 Attributes 字段）
    - crates/nova_character/src/lib.rs（NovaCharacterPlugin::build() 当前系统列表）
    - crates/nova_character/src/movement.rs（刚创建的文件，确认导出名称）
  </read_first>
  <action>
**步骤 1：修改 crates/nova_character/src/character.rs**

在 `CharacterBundle` struct 中新增两个字段：
1. `pub attributes: Attributes`（如果当前 CharacterBundle 没有此字段 — 确认后添加）
2. `pub prev_state: PreviousCharacterState`

修改后的 CharacterBundle：
```rust
use crate::movement::PreviousCharacterState;
// 确保 use crate::attributes::Attributes; 已在文件顶部导入

#[derive(Bundle, Default)]
pub struct CharacterBundle {
    pub stats: CharacterStats,
    pub state: CharacterState,
    pub attributes: Attributes,        // 若已有则跳过；若无则新增
    pub transform: Transform,
    pub visibility: Visibility,
    pub prev_state: PreviousCharacterState,  // 新增
}
```

`PreviousCharacterState` 已派生 `Default`（`was_moving: false`），Bundle derive 要求所有字段实现 Default，满足。

**步骤 2：修改 crates/nova_character/src/lib.rs**

1. 在顶部模块声明处新增：`pub mod movement;`
2. 在顶层重导出处新增：`pub use movement::PreviousCharacterState;`
3. 在 `NovaCharacterPlugin::build()` 的 `add_systems(Update, (...))` 中新增两个系统：

```rust
// 修改前：
.add_systems(
    Update,
    (
        state::stun_tick_system,
        feedback::damage_number_system,
        feedback::hit_flash_system,
    ),
);

// 修改后：
.add_systems(
    Update,
    (
        movement::update_previous_state_system,
        movement::movement_system.after(movement::update_previous_state_system),
        state::stun_tick_system,
        feedback::damage_number_system,
        feedback::hit_flash_system,
    ),
);
```

4. 在 `build()` 函数内注册新类型（可选但推荐）：
```rust
app.register_type::<movement::PreviousCharacterState>()
```
  </action>
  <verify>
    <automated>cargo check -p nova_character 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - crates/nova_character/src/character.rs 包含 `pub prev_state: PreviousCharacterState`
    - crates/nova_character/src/lib.rs 包含 `pub mod movement;`
    - crates/nova_character/src/lib.rs 包含 `movement::update_previous_state_system`
    - crates/nova_character/src/lib.rs 包含 `movement::movement_system.after(movement::update_previous_state_system)`
    - `cargo check -p nova_character` 无编译错误
    - `cargo check --all-targets` 无编译错误（确认下游 crate 不破坏）
  </acceptance_criteria>
  <done>NovaCharacterPlugin 注册了两个系统，CharacterBundle 包含 PreviousCharacterState 字段，整体编译通过</done>
</task>

</tasks>

<verification>
```bash
# 最终验证：nova_character 编译通过
cargo check -p nova_character

# 全项目编译无破坏
cargo check --all-targets

# 单元测试通过（movement.rs 内的 #[cfg(test)] 如果有的话）
cargo test -p nova_character
```
</verification>

<success_criteria>
1. `cargo check -p nova_character` 零错误零警告
2. `cargo check --all-targets` 零编译错误
3. `crates/nova_character/src/movement.rs` 存在，包含 `PreviousCharacterState`、`update_previous_state_system`、`movement_system`
4. `CharacterBundle` 包含 `prev_state: PreviousCharacterState` 字段
5. `NovaCharacterPlugin::build()` 注册了 `update_previous_state_system` 和 `movement_system.after(update_previous_state_system)`
6. `nova_character/Cargo.toml` 包含 `nova_map = { workspace = true }`
</success_criteria>

<output>
完成后创建 `.planning/phases/02-角色移动系统/02-01-SUMMARY.md`，记录：
- 创建/修改的文件列表
- 实际实现的关键决策（PreviousCharacterState 存储形式、Query 结构）
- `cargo check --all-targets` 通过的确认
- 任何与计划的偏差
</output>
